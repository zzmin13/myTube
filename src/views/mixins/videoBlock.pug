mixin videoBlock(video = {})
    .videoBlock
        a(href=routes.videoDetail(video.id))
            video.videoBlock__thumbnail(src=video.videoFile, autoplay=false)
            .videoBlock__info
                if !video.creatorAvatar
                    img.videoBlock__creatorAvatar(src="https://mytube1213.s3.ap-northeast-2.amazonaws.com/avatar/275dacc68a75eb7a1b0b7f1b1b63ef56")
                else
                    img.videoBlock__creatorAvatar(src=video.creatorAvatar)
                .videoBlock__text
                    h4.videoBlock__title=video.title
                    h4.videoBlock__creatorName=video.creatorName
                    h6.videoBlock__views 조회수 #{video.views}회